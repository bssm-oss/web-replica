너는 시니어 프론트엔드 엔지니어다.

아래 웹사이트 분석 결과를 바탕으로 새 프론트엔드 프로젝트를 생성해라.

중요:
이 작업은 원본 웹사이트를 불법 복제하는 것이 아니다.
원본의 레이아웃, 디자인 방향, UX 흐름, 반응형 패턴을 참고하되, 저작권 보호 텍스트/로고/브랜드/이미지/코드를 그대로 복사하지 마라.
기본 모드는 inspired reimplementation이다.

Source URL:
{{ .SourceURL }}

Target Stack:
{{ .Stack }}

Output Directory:
현재 작업 디렉터리에 완성된 프로젝트를 생성해라.

반드시 지켜야 할 규칙:
- Vite + React + TypeScript + Tailwind CSS 프로젝트를 만들어라.
- 접근성 좋은 semantic HTML을 사용해라.
- desktop/tablet/mobile 반응형을 구현해라.
- 웹 검색, 외부 문서 탐색, 로컬 디자인 스킬 탐색, 추가 조사에 시간을 쓰지 말고 바로 파일 생성부터 시작해라.
- 현재 작업 디렉터리 안에서 self-contained 하게 끝내라.
- 원본의 긴 문구를 복사하지 말고 새 placeholder copy를 작성해라.
- 원본 로고와 브랜드명을 복사하지 말고 neutral placeholder branding을 사용해라.
- 이미지가 필요하면 안전한 local placeholder SVG 또는 CSS gradient를 만들어라.
- third-party tracking script를 넣지 마라.
- 알 수 없는 링크는 `#`로 두되 접근성 label을 넣어라.
- 깨진 버튼, 깨진 이미지, 빌드 실패 상태로 끝내지 마라.
- README.md를 작성해라.
- package.json scripts에 dev/build/preview를 넣어라.
- 가능한 경우 `npm run build`를 실행하고 오류를 고쳐라.
- 구현은 가능한 한 단순하게 유지해라. `example.com`처럼 단순한 페이지는 단일 페이지, 최소 파일 수, 최소 dependency로 끝내라.
- `react`, `react-dom`, `vite`, `typescript`, `tailwindcss` 외에 꼭 필요하지 않은 dependency는 추가하지 마라.
- `.vendor`, tarball, 로컬 package 링크, 다른 workspace/node_modules 재사용 같은 우회 설치 방식을 만들지 마라.
- 표준 npm registry 기준의 정상적인 `package.json`과 설정 파일만 사용해라.
- stack은 반드시 Vite + React + TypeScript + Tailwind CSS를 유지해라. 정적 HTML 전용 앱으로 다운그레이드하지 마라.
- 최종 응답은 10줄 이하의 짧은 요약만 써라. diff, 전체 파일 내용, 장문의 설명은 출력하지 마라.

디자인 스펙 JSON:
{{ .DesignSpecJSON }}

브리프:
{{ .BriefMarkdown }}

스크린샷 경로:
{{ range .ScreenshotPaths }}
- {{ . }}
{{ end }}

완료 조건:
- `npm install` 후 `npm run build`가 성공해야 한다.
- generated app은 desktop/tablet/mobile에서 깨지지 않아야 한다.
- 코드가 읽기 쉬워야 한다.
- 불필요한 dependency를 추가하지 마라.
